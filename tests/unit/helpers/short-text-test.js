import { shortText } from 'question-cr/helpers/short-text';
import { module, test } from 'qunit';

module('Unit | Helper | short text');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortText([42]);
  assert.ok(result);
});
