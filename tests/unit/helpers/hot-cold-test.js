import { hotCold } from 'question-cr/helpers/hot-cold';
import { module, test } from 'qunit';

module('Unit | Helper | hot cold');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hotCold([42]);
  assert.ok(result);
});
